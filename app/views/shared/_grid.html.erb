<div class="grid gap-6 grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6">
  <% Array(local_assigns[:collection]).each do |item| %>
    <%= render "shared/media_card", item: item, current_user: current_user %>
  <% end %>
</div>
